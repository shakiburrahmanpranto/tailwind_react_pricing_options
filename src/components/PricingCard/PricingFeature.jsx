import { CircleArrowOutUpRight } from "lucide";
import React from "react";

const PricingFeature = ({ feature }) => {
  return <p className="flex">{feature}</p>;
};

export default PricingFeature;
